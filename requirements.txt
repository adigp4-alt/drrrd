Flask>=2.3.2
APScheduler>=3.10.4
# 1.6.0 (2026-08-13) — Yahoo periodically changes the cookie/crumb handshake
# and older yfinance then returns empty frames rather than erroring. Floor it
# so a fresh install cannot land on a version that is already broken.
yfinance>=1.6.0
pandas>=2.0.3
gunicorn>=21.2.0
openpyxl>=3.1.2
requests>=2.31.0
httpx[socks]>=0.27.0
numpy>=1.25.2
scikit-learn>=1.3.0
vaderSentiment==3.3.2
statsmodels
anthropic>=0.69.0
openai>=1.0.0
mcp>=1.0.0,<3
alpaca-mcp-server>=2.0.0,<3
psycopg[binary]>=3.1
